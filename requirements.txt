# Ядро пересчёта. Коллектор зависимостей НЕ имеет (только stdlib) — см. collector/.
# pandas сознательно не берём: объём данных крошечный, а каждое число должно
# оставаться аудируемым (принцип 7 SPEC).
PyYAML>=6.0
# certifi — только чтобы локальный запуск на macOS не падал на сертификатах;
# в GitHub Actions не требуется, коллектор работает и без него.
certifi>=2024.2.2
